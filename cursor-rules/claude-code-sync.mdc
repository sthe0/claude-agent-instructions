---
description: Global instructions synced from ~/.claude/CLAUDE.md (Claude Code)
alwaysApply: true
---

# Claude Code sync

**Canonical source:** `~/.claude/CLAUDE.md` (symlink → `~/claude-agent-instructions/CLAUDE.md`). On conflict — **CLAUDE.md wins**. Org gates for deepagent: `robot/deepagent/.claude/rules/org-yandex.mdc` (when that repo is open).

Contract: `~/.claude/memory-global/agent-instructions/file-structure-contract.md` — run `verify-layout-contract.sh` after layout changes.

Always consider `~/.claude/CLAUDE.md` and `~/.claude/agents/`. Skills: `~/.claude/skills/` (global instruction skills only). Edit global policy in `~/claude-agent-instructions/`; product/org runbooks in `<project>/.claude/`.

## Habits

Avoid duplicate code; explore before changing; clear code over trivial comments. Python: `~/.venv` unless local memory says otherwise.

**robot/deepagent** → `robot/deepagent/.claude/agent-memory/`. **logos** agents/skills → `logos/.claude/` (local symlinks; not versioned in Arc logos).

## Memory and self-improvement

- **memory** — `~/.claude/memory/INDEX.md`
- **self-improvement** — `~/claude-agent-instructions/`

**Instructions git:** `pull` before edits, **reconcile** after pull (`instructions-git-sync.md` § After pull), `push` after every commit. Cron pull every 10 min.

**Mandatory self-improvement:** substantive user feedback about agent behavior → **Task → self-improvement** same turn (not bare "ok"/"thanks"). User reminder ("why didn't you run self-improvement?") or confirmation to run counts as feedback. See `~/.claude/CLAUDE.md`.

**Mandatory manager:** every **new substantive task** → **Task → manager** first (before planner, developer, mount, broad search). Again on difficulty. See `~/.claude/CLAUDE.md` § Mandatory manager.

## Org work (local memory + org-yandex.mdc)

Do **not** duplicate procedures here. Before Tracker ticket + code, arc mount, or Nirvana WI:

- `~/.cursor/rules/org-yandex.mdc` (gates)
- `~/.claude/memory/claude-code/tracker-ticket-workflow.md`
- `~/.claude/memory/claude-code/arc-parallel-mounts.md`
- `~/.claude/memory/claude-code/nirvana-wi-watch.md`

## Agents (Task)

| Agent | When |
|-------|------|
| **manager** | **First** on new tasks; again on difficulties |
| **planner** | Plan (routed by manager for tickets) |
| **developer** | Production code in isolated worktree |
| **memory** | Runbooks and facts |
| **self-improvement** | User feedback on agent behavior |
| **thinker** | Non-trivial reasoning |
| Optional | `~/.claude/agents/` when `description` matches |

On difficulty — scan `~/.cursor/projects/<project>/agent-transcripts/`. See `agents/manager.md` § Overcoming difficulties.
