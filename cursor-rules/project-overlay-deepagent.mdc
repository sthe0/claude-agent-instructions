---
description: deepagent project overlay — supplements ~/.cursor/rules/claude-code-sync.mdc and ~/.claude/CLAUDE.md
alwaysApply: true
---

# deepagent (project overlay)

Global policy: **`~/.cursor/rules/claude-code-sync.mdc`** and **`~/.claude/CLAUDE.md`** (both from `~/claude-agent-instructions/`). **Do not duplicate** tracker workflow, manager, self-improvement here — they live only in global files.

- **Tool permissions:** `.cursor/cli.json` mirrors `.claude/settings.local.json` in this repository.
- **Domain memory (this product):** leaves via [INDEX](~/.claude/memory/INDEX.md). Do not copy leaves into Arc `CLAUDE.md`.
- **Deepagent MCP:** on request "ask deepagent" — `mcp__yandex__deepagent-generate_answer`, not a subagent.

When editing agent instructions: repository `~/claude-agent-instructions/`, not this overlay.
