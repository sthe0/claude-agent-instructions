---
name: reflexive-exit-is-base-activity-figure
description: The reflexive exit (self-improvement / overcome-difficulty / proactive self-diagnosis) is not a separate mechanism but the base activity figure applied with the norm as material — material=the norm, transform=re-norming, control=achievability of the norm's result, result=a norm adequate to the order — and its search space is the knowledge space = memory.
type: reference
schema: principle/v1
generality: 3
induced_from: [2026-07-14-smd-principle-norm-category-error-and-budget-exhaustion, function-place-difficulty]
created: 2026-07-14
last_verified: 2026-07-14
---

# The reflexive exit is the base activity figure over a norm

## Principle

To keep the whole self-model expressible through the single activity/difficulty apparatus — adding no
parallel machinery — treat every **reflexive** operation (self-improvement, overcoming a difficulty,
proactive self-diagnosis) as the **base activity figure** (the [[plan-activity-ontology]] elements)
applied with **material = the norm itself**. The four load-bearing elements map element-for-element:

<!-- Language exception: норма/перенормирование/заказ/знание are the settled SMD source-ontology terms this principle types; preserved verbatim for traceability. -->
- **material** = the норма (the rule / principle / plan / instruction under revision) — not an object in the world;
- **transformation** = *re-norming* (перенормирование) — the act that repairs the grounding знание and re-derives the norm;
- **control** = achievability of the **norm's result** (does the re-normed rule actually remove the difficulty its functional ground names?);
<!-- Language exception: заказ is the settled SMD source-ontology term; preserved verbatim. -->
- **result** = a norm **adequate to the order** (заказ) it serves — not a norm true in the abstract.

The difficulty (desired ≠ actual) therefore manifests *on the material of the norm itself*.

**Critique = material-analysis of the norm** (the same critique primitive): re-norming is never
wholesale replacement — critique partitions the norm's properties into a **preserve-set** (invariants
that can and must be kept) and a **transform-set** (what must change), exactly as ordinary
material-selection picks material whose required-but-unsatisfied properties are changeable. Mechanized
in `skills/overcome-difficulty/SKILL.md` §3 (`--invariant-to-preserve` / `--difference-to-remove`); the
closing re-norming ACT is the `normalization_blockers` gate.

**The reflexive search space IS the knowledge space = memory.** In ordinary activity the material is
sought in an object space; in reflexion the norm-material is sought in **knowledge space**, and that
space is the agent's memory. So organizing memory well (a navigable decomposition — see
[[memory-hierarchy]]) is *instrumental to reflexion*, not housekeeping: it lowers the cost of every
future self-improvement / overcome-difficulty search.

## Generality

Level 3 — a cross-domain invariant. It ranges over every reflexive operation the agent performs on its
own functional elements: self-improvement (editing instructions / skills / config), overcome-difficulty
(repairing a broken plan or norm mid-task), and proactive self-diagnosis (surfacing difficulties in its
own norms before the user reports them). One figure, one difficulty primitive, three applications.

## Induced from

<!-- Language exception: норма/знание are the settled SMD source-ontology terms; preserved verbatim. -->
- [[2026-07-14-smd-principle-norm-category-error-and-budget-exhaustion]] — the correction that a
  principle is a норма (not a знание judged true/false); "material = the norm" is that framing generalized.
- [[function-place-difficulty]] — a difficulty is a symptom whose cause is a broken functional place;
  the reflexive exit reconstructs that place on the norm layer.

## Refutation

<!-- Language exception: норма is the settled SMD source-ontology term; preserved verbatim. -->
As a норма (not a truth-claim), this is refuted by **inadequacy, not falsity**
(`docs/adr/0004-principle-is-always-a-norm.md`): it fails if some reflexive operation **cannot** be
expressed as the base figure over a norm — i.e. applying the four-element mapping blocks the reflexive
goal and forces a parallel mechanism with its own vocabulary. None has appeared; the mapping stays
goal-adequate and the "no new machinery" constraint holds.

## See also

<!-- Language exception: норма is the settled SMD source-ontology term; preserved verbatim. -->
- `docs/adr/0004-principle-is-always-a-norm.md` — a principle is always a норма; the category correction this principle builds on.
- [[function-place-difficulty]] — the functional-place layer beneath a difficulty (the norm's broken place).
- [[plan-activity-ontology]] — the base figure's 8 elements; element 7 already carries the reflexive-reconstruction refutation figure. The same elements with material=the norm describe self-improvement / overcome-difficulty.
- [[result-checked-against-its-result-image]] — the critique primitive at generality 3 (the control element of any figure, reflexive or not).
