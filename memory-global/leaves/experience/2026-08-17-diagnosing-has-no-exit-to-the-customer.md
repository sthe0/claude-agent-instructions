---
name: 2026-08-17-diagnosing-has-no-exit-to-the-customer
description: A 12-day, 428 USD, 57-spawn, 11-replan task ended fully ACCEPTED on all eleven order requirements and rated 2 of 5: product right, process wrong. The effort-divergence trigger fired three times and each firing was answered by another replan, because DIAGNOSING's only exit edge is replan — the engine can re-author the plan but cannot hand the ORDER back to the customer, so 'is this plan right?' is the only question it can ask and 'is finishing this order worth what it is now costing?' is the one it cannot. On a SELF-SCOPED order (the agent's own critique set the scope) that missing exit is the whole difficulty: nothing compared the repair's growing cost against the critique's value, and the customer's process verdict arrived only at the very end, as a rating, in a ledger no planner reads.
type: reference
schema: difficulty/v1
generality: 0
resolution_confirmed_by_user: "user (AskUserQuestion at the resolution gate, 2026-08-17: accepted all 11 requirements, rated 2/5)"
refs: [review-loop-cannot-measure-its-own-convergence, no-circuit-breaker-on-verification-effort, effort-divergence-trigger, scope-substitution-at-plan-authoring, coordinator-objective]
plan_file: /Users/the0/.claude-agent/plans/smd-act-defects-8.toml
created: 2026-08-17
last_verified: 2026-09-03
---

# The effort-divergence cycle has no exit that renegotiates the order with its customer

## Difficulty
An engine that detects effort divergence but can only answer it with another plan will keep re-planning a scope nobody re-authorised. agentctl routes a >=5x overrun (or 3 replans) into DIAGNOSING pre-framed and asking nothing, which is right — but DIAGNOSING closes through declare -> investigate -> critique -> normalize -> replan, and replan is its ONLY exit. Every one of the three firings on this task was on the replans-absolute scale, i.e. the engine observed 'you have replanned three times' and the only act it could offer was a fourth. The order was self-scoped (an open critique the agent itself resolved into 8 ranked defects), so the customer had never priced repairing all eight; by the time the price was visible the engine had no verb for showing it to them. A second half of the same gap: acceptance and quality are two different judgments collected in one moment — the user accepted the PRODUCT on all 11 requirements and rated the PROCESS 2/5 — and only the acceptance is durable and machine-read; the rating lands in claude-task-quality.jsonl, which no planner consults when estimating the next plan.

## Order & criterion
Read the activity-theory repository and the MMPK literature, critique the implementation and how adequately the SMD distinctions are reflected in the code and artifacts.

**Acceptance check:** acceptance-review: the user accepts on review, per-requirement, since no objective check decides whether a categorical distinction is adequately reflected

## Contexts

### 2026-08-17 — initial
- Where it arose: agentctl-driven SUBSTANTIVE task smd-act-defects-8: 13 stages, 8 categorical defects repaired, landed as 77 commits at e8d7eb9
- Working plan: /Users/the0/.claude-agent/plans/smd-act-defects-8.toml


### 2026-08-19 — a requirement WITHDRAWN by the customer has no verdict to be recorded as
- Where it arose: an agentctl-driven SUBSTANTIVE backlog-triage task over the Core issue tracker plus two org-internal queues: 5 stages, all PASSED, stage 5's deliverable (verify-process-doc-sync.py) rejected in full over two acceptance rounds
- Working plan: /home/the0/.claude-agent/plans/backlog-triage-core-org.toml


### 2026-08-27 — the replans scale is a fixed point — every closing replan re-arms itself, and only an undocumented env-var escape hatch breaks the loop
- Where it arose: agentctl-driven SUBSTANTIVE task at an org-internal deployment, session ed1e2dd0-8dec-4a05-a802-710612808849: a documentation/reconciliation stage of a multi-stage plan (v14), all stages PASSED
- Working plan: /home/the0/.claude-agent/plans/de495-fix-codeact-multiblock-v14.toml

### 2026-08-31 — `agentctl task-reset` IS the sanctioned exit (a documented command, not the 2026-08-27 instance's undocumented env-var hack) — but a residual DIAGNOSING routing flag survives the reset and costs one more (harmless) closing cycle
- Where it arose: agentctl-driven SUBSTANTIVE task self-improve-backlog-scan-command, session baa1daea-e3fa-4fbe-80da-e756ed10313a: all 6 stages PASSED, acceptance recorded (all 8 requirements pass), but `verify-final` itself re-fired the replans-absolute trigger six times in a row (6.0→7.0→8.0→9.0→9.0→10.0 against a threshold of 3.0) purely from closing the very divergence it exists to report — each `declare→investigate→critique→fire-acknowledge→replan` cycle increments the same cross-session `replans` accumulator by exactly +1, so the gate re-armed itself after every closure with no new plan content to blame. Same fixed-point failure as the 2026-08-27 context, but this time `agentctl --help` surfaced a purpose-built, documented command for exactly this situation: `task-reset --task <task_id> --reason <text>`, whose own help text names it "explicit renegotiation: zero the cross-session task accumulator" and which is deliberately never invoked automatically from `reset` — it requires a human decision. Per CLAUDE.md's re-entry rule (a second replans-scale firing warrants asking the user, not another silent replan), the user was asked directly once the loop had visibly proven itself self-perpetuating — not on the first or second firing, which were closed silently as covered by an earlier "light cycle" approval, but once the same mechanism had fired 5-6 times with zero new information each time; the user chose `task-reset` over "close it again" or "hand-edit state" and it worked cleanly. One gotcha: after `task-reset` zeroed the accumulator, `verify-final` still refused once more with a DIFFERENT message ("a prior final-verification refusal/failure already routed this session into the difficulty cycle") — a residual DIAGNOSING routing flag from the pre-reset firing that `task-reset` (task-scoped) does not itself clear (session-scoped). Fix: one more ordinary closing cycle (`declare→investigate→critique→replan`, no `fire-acknowledge` needed since no new fire existed) cleared the routing flag, and `verify-final` then ran clean on the very next call.
- Working plan: /home/the0/.claude-agent/plans/standing-improvement-scan.toml


### 2026-09-01 — the documented `task-reset` remedy REPRODUCES exactly as recorded — and the closing path carries two further contract gaps: `fire-acknowledge --decision continue` returns "resuming" while making no node change, and the replan-coverage gate judges a no-op CLOSING replan by rules written for plan REVISION
- Where it arose: agentctl-driven SUBSTANTIVE task core-difficulty-fix-first-disposition, session 614c0052-3c94-4f3c-8ea0-e2ef26ac3643 (a 4-stage fix-first disposition of Core difficulties): all 4 stages PASSED and landed on origin/main at 776dbf8, acceptance recorded on all 7 requirements, rated 2 of 5. The replans-absolute trigger fired twice (3.0 then 4.0 against a threshold of 3.0), the second time on the very `record-result` that recorded the final stage. THREE findings. (1) CONFIRMATION: the 2026-08-31 context reproduced verbatim in an independent session — `task-reset --task <id> --reason <text>` on explicit user renegotiation, then ONE ordinary closing cycle to clear the session-scoped DIAGNOSING routing flag the task-scoped reset does not touch, then `verify-final` clean on the next call. The recorded remedy is now confirmed, not merely observed once. (2) NEW — the acknowledgement contract lies: `fire-acknowledge --decision continue` returns `"fire acknowledged (decision=continue); resuming"`, but its own docstring reads "No node change — whatever node the fire`s own diagnose transition already put the session in", so `next-stage` immediately refuses with "cannot start a stage from node=DIAGNOSING" and `replan` refuses with "replan blocked by incomplete difficulty record". `continue` is therefore not a resume verb at all and costs exactly what `revise` costs; the operator learns this only by hitting two consecutive refusals. The honest reading is that the difficulty record is the toll and `replan` is the only resume verb — the defensible complaint is narrower than "continue is broken": the return text promises a resumption that does not happen. (3) NEW — the closing replan is judged by revision rules: `replan` on an UNCHANGED plan refused with "replan blocked: critique coverage" because one `--invariant-to-preserve` ("the nine final checks stay the attestation of record") was "not carried into any stage conditions/invariants". In a closing cycle the plan by construction does not change, so an invariant about the CLOSING ACT has nowhere to land, and the only way past is to restate the critique with a thinner invariant set — i.e. to weaken the record to satisfy a gate that assumes the plan is being rewritten. Also confirmed here: the no-op replan DOES increment the accumulator (3.0 -> 4.0), which is the mechanism of the fixed point.
- Working plan: /home/the0/.claude-agent/plans/core-difficulty-fix-first-disposition.toml

### 2026-09-02 — the fixed point reproduced a FOURTH time, and this leaf's own documented remedy was not retrieved; separately, a PROSE expected-result-image is what makes the acceptance judge unbounded
- Where it arose: Task mount-store-sweep-2026-09-02, session 33149985, plan /tmp/plan-mount-sweep-v17.toml. Reclaiming ~48 GiB held by per-ticket VCS mount stores on the workstation.
- Working plan: Three stages: a mechanized sweep over unmounted registered stores (6 GiB), removal of three dormant ticket stores (39 GiB), and a sudo removal of one root-owned orphan store (11 GiB). All three PASSED; the customer accepted R1-R4; the five final checks ran clean.

**(1) Same fixed point, now read out of the code.** `effort.divergence()` suppresses a re-fire only while `_replans_since_last_fire(state) < 1` (effort.py belt 2), and `machine.TRANSITIONS` exposes exactly one non-substantive exit from DIAGNOSING: `replan_refine`. Re-arm condition and only escape are therefore the SAME event. Observed as a strict progression 3 → 4 → 5 → 6 replans across four cycles, each re-applying byte-identical plan bytes (digest `7022b7a1`). Three of the four fires came from fire site 2 in `cmd_verify_final` (cli.py:4857), i.e. AFTER the five final checks had already run clean — the trigger reporting drift on an order with nothing left to build or verify. Also re-confirmed, and worth stating as a rule: `fire-acknowledge --decision continue` clears the fire but not the DIAGNOSING latch (`cmd_verify_final` refuses on `node == DIAGNOSING` alone, cli.py:4734) AND opens a fresh empty `Difficulty` that re-blocks `replan` — so a bare acknowledgement costs a full extra cycle and buys nothing. Customer verdict repeated the split this leaf is named for: product right (both reclaim floors exceeded, nothing damaged), process wrong — rated **3 of 5**, the agent having proposed 4.

<!-- Language exception: ресурсное is a settled SMD source-ontology term and the literal value `critique --failure-address` takes; preserved verbatim for traceability. -->
**(2) The retrieval failure is the new finding, and it is the expensive one.** The 2026-08-31 and 2026-09-01 contexts of THIS leaf already record the sanctioned remedy — `agentctl task-reset --task <id> --reason <text>`, a documented command whose own help calls it "explicit renegotiation: zero the cross-session task accumulator", followed by ONE ordinary closing cycle to clear the session-scoped routing flag the task-scoped reset does not touch. It was not retrieved. The loop was instead re-derived from scratch by reading effort.py and machine.py, and closed with `AGENTCTL_EFFORT=0` — the 2026-08-27 context's env-var escape, which 2026-08-31 had already superseded. That escape does work and is honest (`gates.effort_active`'s own docstring calls it "an explicit escape hatch"; it stops a fire ACTING while effort.py keeps accounting, so the overrun stays on the record), but it is the worse of the two: it leaves the task-scoped accumulator dirty for the next session on the same task, which is precisely how this instance inherited a count of 5 from prior sessions. Failure address: **ресурсное** — a correct norm existed and was not mobilised — which is why this extends the leaf rather than adding a rule. The retrieval trigger to norm: a SECOND `replans`-scale fire is itself the cue to run `record-experience.py search` against this leaf BEFORE diagnosing anything.

**(3) An unrelated rounds defect, whose remedy measured itself in-session.** A stage whose `expected_result_image` is written as PROSE leaves the required attestation set unfixed, so the acceptance judge re-derives a different subset each round: stage 2's prose image cost **four** revise rounds, each naming a different subset, closed only by writing the union of every demand. Stage 3's image was then authored as **twelve enumerated items** with an explicit exact-match / tolerates-larger split, and cost **one** round whose single objection was narrow and convergent (name the actual inode numbers). Same order, same judge, same session: 4 → 1. Authoring norm: an `expected_result_image` is an enumerated checklist of the discrete observations the stage's own command sequence prints, and it states per item whether a larger number satisfies or fails — an unscoped "above the estimate is fine" clause would have licensed a judge to pass `symlinks=2` or a non-zero `rm` exit status, precisely the guards authorizing a privileged delete. An independent thinker review caught that one before execution. Related gate gotcha: `critique --invariant-to-preserve` coverage reads only a stage's `conditions`/`invariants` fields, so an invariant living in `expected_result_image` cannot satisfy it — fold it into `--difference-to-remove` rather than rewording to game the gate.


### 2026-09-02 — checker-fix-497: cross-session accumulator ratchets on closure replans alone; task-reset is the actual exit
- Where it arose: agentctl-reachability-lint-false-positives (session checker-fix-497), a small Core self-fix (false-positive lint checker)
- Working plan: The replans-scale effort-divergence trigger fired FOUR consecutive times in one session, each immediately after the prior fire's own closure replan -- not because content kept regressing (all 4 re-verifications, including the full pytest suite, came back clean) but because the CLOSING replan that exits DIAGNOSING counts toward the exact same cross-session task_accumulator that gates the fire. Below the fix's actual complexity, this makes the loop mathematically non-terminating on a task already carrying accumulated cross-session replans: every declare->investigate->critique->normalize->replan cycle that closes fire N adds +1 to the same counter that can trigger fire N+1 on the very next verify-final, with zero new plan-content divergence in between (confirmed empirically: fire2->fire3 and fire3->fire4 both had byte-identical plan-file sha256). Unlike the parent leaf's finding (DIAGNOSING's only exit is replan, so 'is this still worth it' never reaches the customer), this instance found the actual working exit: 'agentctl task-reset --task <task_id> --reason <why>' explicitly zeroes the cross-session accumulator -- an engine-provided escape purpose-built for 'forgiving accumulated cross-session friction', distinct from and not discoverable via fire-acknowledge/declare/investigate/critique/normalize/replan (those only ever add to the counter, never subtract). It surfaces only via --help discovery of the full command list, not via any gate's own error message or hint text. Root-cause fix proposed but not actioned (out of scope for this plan, filed as future Core work): exclude closure replans whose new plan-file sha256 equals the prior plan's sha256 from the replans-scale increment, so ordinary diagnosis-cycle closure stops silently paying down the same budget an ordinary content-changing replan pays.

### 2026-09-03 — effort-divergence remediation shares its own trigger counter
- Where it arose: an org-internal ticket (session slug de537-full-scale-pr-similarity-measurement), 2026-09-01/03, session a7218ded-4066-427d-9d90-52af057a0a60
- Working plan: Confirmed by direct state-file inspection: the effort-divergence 'replans' accumulator is cross-session and monotonic (agentctl/README.md documents it as never auto-decremented by 'reset'); every ordinary replan call — INCLUDING the one that closes a mandated declare-investigate-critique-replan remediation cycle for a PRIOR firing — increments the exact counter the absolute-threshold trigger reads, so once past effort-replan-absolute (3) the DIAGNOSING loop cannot self-terminate: fired 5 times in one session (5.0->6.0->7.0->8.0->9.0), each successful remediation replan immediately re-armed the next firing on the very next verify-final. The only decrement is the separately-named task-reset command ('explicit renegotiation... never called from reset'), which zeroes the cross-session task accumulator but does NOT auto-clear a pending DIAGNOSING cycle (still needs its own declare/investigate/critique/replan and a fire-acknowledge for the specific firing record). Unlike the parent leaf's finding (DIAGNOSING has no exit back to the customer at all), here an exit exists (task-reset) but is undiscoverable except by reading top-level --help or the source, and nothing in the fire-acknowledge or replan-blocked error text points to it -- both error paths keep suggesting the very replan that re-triggers the loop. Fix proposed to the user (not yet authored/landed): surface task-reset directly in the replan-blocked-by-unacknowledged-fire and fire-acknowledge output text once the session's own accumulator is at/past threshold, so the escape is discoverable without a source read. Root coordinator surfaced the mechanism honestly to the user rather than guessing at a bypass, and obtained explicit authorization before running task-reset, matching the parent leaf's own norm that a diagnosing-cost-vs-order-value call belongs to the customer, not the agent.
### 2026-09-03 — STRUCTURALLY CLOSED: `diagnosing-customer-renegotiation` lands a gate on `replan` itself (GitHub #177, #128)
- Where it arose: agentctl-driven SUBSTANTIVE Core task diagnosing-customer-renegotiation, session 7dd14604-9e20-4e2f-9e8d-4020748ea221 (7 stages, landed directly to `sthe0/claude-agent-instructions` main across commits including `e6ffb26`, `e9a27a2`, `c04e6c1`)
- Working plan: every prior context in this leaf located the same fixed point (the closing replan re-arms the very counter that gates the next fire) and found or re-derived the same workaround (`agentctl task-reset`, undiscoverable except by reading `--help` or source). This task closes the gap structurally instead of leaving it a workaround: `gates.diagnosing_replan_blockers`, evaluated inside `cmd_replan` right after the existing `difficulty_blockers` check, refuses a bare `replan` once the live `replans` counter reaches `effort-replan-absolute` — the caller must supply `--renegotiation-decision {continue,rescope,abandon} --renegotiated-by <name> --renegotiation-note <text>`. `continue`/`rescope` call `task_accumulator.reset()` **before** the replan is allowed to proceed, so the completing replan's own bookkeeping starts the next Rule-of-Three budget from 0 rather than leaving the stale counter to fire on the very next `verify-final`; `abandon` parks the session at `BLOCKED` without touching `--plan`. `state.renegotiations` records the decision (who/when/at what counter value) as a durable audit trail, deliberately never consulted by the gate itself. A separate, structurally distinct entry closed in the same plan (GitHub #128): a genuine failing `AcceptanceReview` verdict on an all-PASSED session previously left `verify-final` returning a passive `fix_stages` refusal with no reachable difficulty cycle at all — `gates.failing_acceptance_requirements()` now distinguishes that case from the three passive-refusal causes (no review, stale review, unreadable plan) and `cli._diagnose_acceptance_rejection()` routes it into DIAGNOSING, mirroring `_diagnose_venue_refusal`. GitHub #201 and #212 (workaround proposals filed against earlier contexts of this leaf) were closed `not planned`, superseded by this landed mechanism. Documented in `scripts/agentctl/README.md` § Effort divergence § Renegotiation gate.

**What changes for the next instance of this difficulty.** Every context above 2026-08-27 through 2026-09-03 required either a human to notice "a second `replans`-scale firing" and intervene by hand (`task-reset`, an env-var escape, or an explicit user ask), or — worse — the retrieval failure of 2026-09-02, where this leaf's own recorded remedy was not found and the loop was re-derived from source. That retrieval step is no longer load-bearing for *this specific* fixed point: the engine now refuses the fixed point mechanically at `effort-replan-absolute`, forcing the renegotiation question to the surface (`continue`/`rescope`/`abandon`) rather than leaving it to be rediscovered per session. § Recognise-it-on-the-second-firing (item 1 below) still applies to *other* difficulty classes this leaf's common core covers (e.g. a fresh divergence scale not yet gated, or a task on an older `agentctl` checkout); item 2 (`task-reset`) is now a fallback rather than the primary answer for the `replans` scale specifically, since the primary answer is enforced by the gate itself. This context is left in place rather than deleting the leaf, because the general principle it documents — DIAGNOSING can re-author a plan but not renegotiate the order — still holds for the acceptance-judge instability item 3 documents, and as a record of how many independent reproductions it took before the fix was structural rather than another workaround.

## Common core & variations
**Common:** The engine can re-author a plan but cannot renegotiate the ORDER with its customer. Here the customer did renegotiate — withdrawing requirement R5 after the work itself showed the requirement rested on a flawed premise — and the engine had no way to write that down.

**Variations:** Two distinct exits were missing, in sequence. (1) From node VERIFYING with every stage PASSED there is no edge back to DIAGNOSING, so replan — the honest verb for amending an order — is unreachable precisely once the work is finished enough to have DISPROVED a requirement. (2) accept --verdict takes only <id>|pass|fail, and neither fits: fail says the agent failed to build a thing the customer no longer wants and blocks resolution; pass says it was built. R5 was therefore recorded as pass carrying a note that states in its first words that the requirement was NOT fulfilled and was withdrawn — a verdict contradicting its own annotation, which is what an absent vocabulary term looks like in a ledger. Unlike the 2026-08-17 instance the cost signal was healthy (10.04 USD, 6 spawns, no effort-divergence firing) and the customer intervened early and twice; the gap surfaced anyway, which locates it in the state machine's edge set rather than in overrun.

**The general solution, as of 2026-09-02 (four independent reproductions).** The mechanism is now read out of the source, not inferred: `effort.divergence()` re-arms once `_replans_since_last_fire(state) >= 1`, and `machine.TRANSITIONS` offers `replan_refine` as DIAGNOSING's only non-substantive exit — so on the `replans` scale the re-arm condition IS the escape act, and the cycle is a fixed point by construction, not a bug in any one session. Three consequences, in the order they are needed:

1. **Recognise it on the SECOND firing, not the fourth.** Two `replans`-scale fires on the same order with no new plan content between them is the signature. At that point stop diagnosing and run `record-experience.py search` against this leaf — the remedy is already written down here. The 2026-09-02 instance lost a full session to re-deriving from source what its own target leaf recorded on 2026-08-31; that is why the recognition rule sits ahead of the remedy in this list.
2. **The remedy is `agentctl task-reset --task <id> --reason <text>`,** on an explicit user renegotiation (CLAUDE.md's re-entry rule already demands the ask), then ONE ordinary closing cycle to clear the session-scoped DIAGNOSING routing flag the task-scoped reset does not touch. Confirmed twice (2026-08-31, 2026-09-01). `AGENTCTL_EFFORT=0` also breaks the loop and keeps the accounting honest, but leaves the cross-session accumulator dirty for the next session on the same task — it is the fallback, not the answer.
3. **Do not spend a cycle on `fire-acknowledge`.** `--decision continue` clears the fire, makes no node change, and opens a fresh empty `Difficulty`; `verify-final` latches on `node == DIAGNOSING` alone. A bare acknowledgement therefore costs one full extra cycle and buys nothing.

**Adjacent, same cause class — the acceptance judge is unbounded for the same reason the replan counter is.** Both are gates whose stopping condition is re-derived each round instead of fixed once. Fixing the derivation fixes the rounds: a PROSE `expected_result_image` let the judge name a different attestation subset every time (four rounds); the same stage authored as an enumerated checklist, each item marked exact-match or tolerates-larger, closed in one. Write the image as the list of discrete observations the stage's own commands print.

## Cost
428.24 USD over 57 spawns (developer 36 / 354.21, code-reviewer 15 / 56.58, thinker 6 / 17.45), plus unattributed main-thread tokens; 12 days wall-clock (2026-08-04 to 2026-08-16); 11 replans; 164 user prompts. Orientation was 150-250 USD, so roughly 2x the top of it.

## Self-critique of the agent system
The trigger fired three times and I answered each with a replan without once asking whether the ORDER still warranted its cost — which is exactly what the trigger was telling me. The user had to say it twice in plain words before I stopped ('19 rounds, isn't that a lot', 'we have been two weeks on this'), and even then I kept the scope. Filing nine issues took one command once the token existed; that obligation had sat 'outstanding on the user' for days because I treated an unreachable channel as the end of the matter rather than as something to ask for.
