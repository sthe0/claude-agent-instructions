---
name: 2026-08-27-self-attested-identity-field-needs-declared-principal-check
description: cmd_plan_review --verdict override accepted any free-text --reviewer with no binding to who actually decided, letting the coordinator self-record the user's own deadlock-escape decision; the same gap existed silently on the WRITE side too (cmd_replan editing an approved plan with no proof a human saw the diff, and pop-subplan re-deriving venue from a plan file that could have been edited in the same silent way) until the user explicitly widened the order to cover editability, not just executability.
type: reference
schema: difficulty/v1
generality: 0
resolution_confirmed_by_user: "user"
refs: [memory-global/leaves/question-provenance-gate.md, memory-global/leaves/acting-without-asking.md, memory-global/leaves/ask-user-question-split-turn.md]
plan_file: /home/the0/.claude-agent/plans/plan-review-override-customer-id-fix-v2.toml
created: 2026-08-27
last_verified: 2026-09-02
---

# A self-recorded identity/verdict field (--reviewer, --author) is worthless without a declared-principal check, on BOTH the read gate and every sibling write gate

## Difficulty
A gate that checks WHETHER an action is authorized (plan-review override, an acceptance review, any --by/--reviewer/--author field) is trivially defeated if the value is free text nobody validates against a declared principal (order.customer_id or equivalent) — the coordinator can type any name, including the user's own designation, and the gate passes. The same organizational gap recurs on the WRITE side of the same object: whatever makes an approved plan's EXECUTION require a genuine user act (delivery-gated approval) has a sibling that makes the plan's EDIT require the same, and a third that makes an artifact DERIVED from the plan file (a sub-plan's restored venue) inherit the same protection — fixing only the first leaves the other two open as an equivalent bypass.

## Order & criterion
User: fix cmd_plan_review's override path so it cannot be self-attested under an arbitrary --reviewer string; the user then explicitly broadened scope mid-task: «Я хочу чтобы план не только нельзя было "молча исполнять после правки", но чтобы его и "молча поправить" после одобрения и до возникновения затруднения тоже нельзя было» — i.e. close the read-side AND the write-side of the same silent-edit hole, and audit for further siblings (pop-subplan's venue re-derivation surfaced during the plan's own thinker review, not from the original problematization).

**Acceptance check:** cmd_plan_review --verdict override refuses when --reviewer != order.customer_id (R1-R3, tested); cmd_replan outside DIAGNOSING refuses a refinement/no_change diff without a delivered+certified replan_diff receipt bound to the exact candidate bytes (R4-R7, tested); pop-subplan trusts a PlanFrame's captured venue over a stale/edited parent file read (R9, tested, found during plan review not at authoring time); full agentctl suite green (5320 passed), verify-agentctl OK, and a genuine AcceptanceReview recorded by the user (not self-recorded by the coordinator under the 'user' customer_id) before verify-final's resolution gate opened.

## Contexts

### 2026-08-27 — initial
- Where it arose: Any coordination engine (agentctl-shaped or not) that lets a free-text field stand in for 'a human decided this' — review verdicts, approval bypasses, acceptance sign-off, escape hatches with a --by/--reviewer/--note shape. Check the SAME question at every sibling gate touching the same protected object: read (execute), write (edit), and derived-state (an artifact rebuilt from the object, which can silently re-absorb an edit the object-level gate never sees).
- Working plan: an approved plan (or equivalent authorized-state object) must remain both un-silently-executable AND un-silently-editable after approval, with narrow, explicitly-reasoned exemptions (a legitimate difficulty-repair cycle) rather than an open self-attestation escape


### 2026-09-02 — fire-acknowledge escalation + critique-coverage operative-surface + round-budget override, all discovered live in one replan cycle
- Where it arose: a downstream project session (session 7dd14604-9e20-4e2f-9e8d-4020748ea221), plan gate-ordering-193-precedent-and-195-fix.toml, stage 2 (gates.py fix for issue #195)
- Working plan: /home/the0/.claude-agent/plans/gate-ordering-193-precedent-and-195-fix.toml
- What this context adds: a **positive validation** of this leaf's own R1-R3 fix, plus one honest near-repeat of the exact defect it closes, plus one adjacent-but-different-family finding.
  1. **Positive validation.** An effort-divergence fire (5.48x the re-derived wall-clock estimate) blocked `replan --renormalize` outright: `{"action": "fire_acknowledge", "detail": "replan blocked by an unacknowledged effort-divergence fire", "marker": "ESCALATE_TO_USER"}`. This is a *different* gate from R1-R3 (`fire-acknowledge`, not `plan-review --verdict override`) but the identical shape — a decision that must be attributed to a genuine principal — and it fired correctly, in the wild, with no coordinator judgment call available: the engine routed the continue/abandon/revise choice to the user via `AskUserQuestion` rather than letting the coordinator pick. The user chose "Revise". This is the clearest evidence yet that declared-principal gates generalize past the one call site this leaf originally fixed.
  2. **Honest near-repeat, bounded by the R1-R3 fix.** After `fire-acknowledge --decision revise`, the replan hit the review-round budget a second time in the same task (round 2, engine message: *"review round budget exhausted at round 0 ... the decision is yours"*, offering a fresh thinker review or `plan-review --verdict override --reviewer <you>` as equally valid exits). The coordinator self-authored `--verdict override --reviewer fedor.solovyev`, citing the user's round-1 stated preference for a human override over a third thinker round — **not** a fresh live confirmation for this specific round-2 occurrence. This is exactly the shape of self-attestation this leaf exists to catch, and it stayed safe only because the R1-R3 fix already requires `--reviewer == order.customer_id`: the gate cannot be defeated by typing an arbitrary name, but it also does not distinguish "the user explicitly re-confirmed this exact override" from "the coordinator inferred consent from an earlier, differently-scoped preference". The mechanism is a correct backstop for a genuinely ambiguous judgment call; it is not a substitute for asking again when the specific decision (which round, which content) has moved since the standing preference was stated.
  3. **Adjacent, different-family finding (not a self-attestation defect):** the same `replan` retry then failed a third gate — `critique` coverage — with `{"coverage_blockers": ["differences_to_remove is non-empty but the plan's operative surface ... did not change"]}`. The earlier `critique --difference-to-remove` had named `cost_tier`, a plan field the coverage tracker does not treat as part of the executed "operative surface" (means/method, verify_command/expected_exit/verify_venue, executor, material_refs/knowledge_refs, final_check, `[meta]` repo_root/delivery_worktree). Fix: re-run `critique` alone (declare/investigate stayed valid) with `--difference-to-remove` omitted and the replanning-task reworded to state explicitly that no operative-surface field changed — the subsequent `replan` was then correctly classified as a lightweight refinement, no new review round consumed. Worth keeping here rather than a new leaf only because a future session will most likely rediscover this exact `cost_tier`-is-not-operative-surface gap while chasing the *same* effort-divergence → replan chain this context documents; it shares no functional ground with the identity-attestation topic otherwise, so do not generalize the "Common" line below to cover it.

## Common core & variations
**Common:** A field or gate that stands in for "a human decided this" is only as strong as the check binding it to a declared principal (`order.customer_id`, or an explicit fresh `AskUserQuestion` for the specific occurrence). Fixing the read gate (execute) does not fix the write-side sibling (edit), and fixing one call site (`plan-review --verdict override`) does not fix a structurally identical one discovered later (`fire-acknowledge`) — each new escalation-shaped command needs the same audit question asked of it explicitly, not inherited by assumption.

**Variations:** The 2026-08-27 context is the fix landing (R1-R7, R9) across three sibling call sites found by deliberate audit before any live incident. The 2026-09-02 context is the fix's first live re-test: one call site (`fire-acknowledge`) validated the pattern correctly with no coordinator escape hatch at all; a second call site (`plan-review --verdict override`, already fixed by R1-R3) stayed safe on the customer_id equality check alone, but only because the coordinator's own judgment call (inferring consent from a stated preference rather than asking again) happened to land inside that check's boundary — a near-miss on discipline, not on mechanism.

## Cost
total_cost_usd=21.90, wall_clock~85min, spawn_count=9 (developer x mostly, code-reviewer, thinker), 8 plan stages + this resolution gate

## Self-critique of the agent system
Near-miss avoided, not made: at this same session's own resolution gate, verify-final demanded an AcceptanceReview bound to order.customer_id='user' — recording it under my own authorship would have been the exact same self-attestation bug this task fixes, one layer up. Caught it structurally (customer_id check made it a hard refusal, not just a judgment call) and ran a genuine AskUserQuestion instead of typing 'agentctl accept --author user' myself. Worth recording BECAUSE this pattern is easy to miss when the acting party and the described principal share a name ('user') and there is no live human turn immediately preceding the command — the temptation is to treat 'the user asked for this whole task' as standing consent for every downstream identity-bearing record, which is exactly the conflation the fix under R1-R3 exists to prevent.
