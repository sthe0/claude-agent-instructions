---
name: 2026-06-04-verify-load-bearing-axis
description: Recurring difficulty — a surface signal (daemon status, a confident research/web claim, a green static check, a prior plan's assumption) is trusted as evidence of the load-bearing fact, while the axis that actually matters tells a different story. Fix is to observe that axis directly before concluding.
type: reference
schema: difficulty/v1
resolution_confirmed_by_user: "<migrated 2026-06-11 to difficulty/v1; per-context confirmations preserved inline>"
refs: [2026-05-26-agent-system-plan-vs-reality-drift]
created: 2026-06-11
last_verified: 2026-07-16
---

# Verify the load-bearing axis, not the surface signal

## Difficulty
A proxy signal reads "fine" and is taken as proof of the underlying fact: a daemon is `active` and `getMe` returns 200, a research agent states a version-specific claim confidently, a static check (imports/tests/build-diff) is green, or a prior plan's central assumption is reused — while the *load-bearing* axis (background loops alive, primary-doc-confirmed, runtime behavior, measured data) is never checked and turns out to disagree.

## Order & criterion
Before acting or concluding, observe the axis that actually carries the conclusion — not the proxy. **Acceptance check:** the conclusion rests on a direct observation of the load-bearing fact (disk + log grep, a primary-doc `WebFetch`, a measurement over real data, a runtime test of the affected path), and a transient proxy blip is retried before being read as a state.

**Co-located-representation sweep (canonical-identifier change).** When the change sets a *default* or *canonical identifier* (model name, path, flag, tokenizer), completeness is load-bearing: before declaring done, **proactively** sweep every co-located representation — (i) runtime call-sites (is the default even live, or does the value come from a registry/config?), (ii) sibling/variant strings of the same identifier family (grep the family — `Foo` vs `Foo-Instruct-2507`), (iii) infra preload / baked caches under offline (`Dockerfile`, image warmup), (iv) related consumers (judge / dataset / metrics). Drift left in any one of these is the reviewer catching your incompleteness, not yours to leave for them (DEEPAGENT-433: four such catches in one session).

## Contexts

### 2026-06-04 — confident-but-wrong research; assumption overturned by measurement
Confirmed: "Да, решено". A web-research sub-agent asserted `MAX_THINKING_TOKENS` was removed on Opus 4.8 (the official costs doc still documents it) and surfaced env names from community guides. Resolved by a verification pass against the official `code.claude.com/docs` pages before touching settings — confirmed two env vars, dropped an unconfirmed one. Separately, a 30-line transcript aggregator overturned the prior plan's central assumption: the static prefix is ~5–10% of cost; ~71% of `cache_read` is session turns beyond #200 → had to *reframe* (long sessions on the 1M window), not extend. The research agent's own adversarial flags were accurate — heed them. Commit `ef028d5`.

### 2026-06-05 — healthy-looking daemon, dead background loops
Confirmed: "Да, пришёл — закрываем". ccgram "связь отвалилась" with `systemctl status` = `active` (1wk+ uptime) and `getMe` = 200, yet the bridge was degraded: `OSError: [Errno 28] No space left on device` had crashed the background poll loops, which do not self-resurrect (only a restart revives them). The health checks pointed away from the symptom the user felt. Fix: grep the log for ENOSPC + `df` *before* trusting `status`/`doctor`; a single `getMe` timeout that 200s on retry is a transient IPv6 egress blip, not an outage. Built a systemd-user watchdog. Diagnostic-order lesson now in the ccgram-management skill.


### 2026-06-16 — DEEPAGENT-433: 'default' change whose runtime effect must be located, not assumed
- Where it arose: robot/deepagent eval/convert/metrics — flipping Qwen3-14B→Qwen3-3A-30B defaults; verifying the change actually takes effect
- Working plan: Three surface-vs-load-bearing splits, each resolved by observing the real axis: (1) **enum default ≠ runtime effect** — the vh3.Enum model_architecture default (operations.py) looked like 'the convert default', but every prod call-site passes model_architecture explicitly from the registry (functions.py:455-473 options=model_conversion_kwargs); grepping call-sites showed only the metrics tokenizer_type default is actually runtime-effective (compute_metrics/render call-sites pass nothing). Verify call-sites before claiming a default change matters. (2) **green WI ≠ tokenizer loaded** — compute_metrics loads the tokenizer inside try/except (a failure only warns), so a success WI does not prove the new 30B tokenizer loaded; the load-bearing observable is the block stderr line 'Load tokenizer: Qwen/Qwen3-30B-A3B-Instruct-2507' + 'Updated row with auxiliary metrics', read via getBlockLogs→stderr.log (NOT the ui-api-proxy path, which 404s on the public blockGuid). (3) **static build ≠ offline runtime** — files are Docker-packaged (no ya-make test target), and the image preloads tokenizers under HF_HUB_OFFLINE=1; a default tokenizer string that is NOT in the Dockerfile preload list would fail at runtime offline. Validated by an in-graph image build (--images <commit>:arc_ref=<commit>) from the Dockerfile-change commit + offline run_eval whose compute_metrics loaded -Instruct-2507 from the baked cache.

### 2026-06-16 — DEEPAGENT-426 trained-model inference: degenerate all-yes — bug or sample bias?
- Where it arose: A structurally-green trained smoke on 5 synthetic DEEPAGENTSUP tickets emitted all-yes. The surface signal (green WI + 'plausible distribution' DoD) could not tell a correct output from a degenerate one — the sample had no known-negative, so all-yes was unfalsifiable. The load-bearing axis (agreement with labeled ground truth) was never observed.
- Working plan: Located the parent ticket DEEPAGENT-413's labeled train/test data (//home/deepagent/ruslanzalikov/iron_intern/datasets: origin_data n=100, held-out desc_mut_test n=20, ~64pct negative). Ran op_classify_trained on the 20 real held-out tickets (13 no / 7 yes) via a throwaway classify-only graph, bypassing Tracker fetch to feed the exact stored text. Result: mixed distribution — only 4/20 yes, 8/13 true-no to no, 20 distinct scores; verified independently against the predictions table. Verdict: all-yes was SYNTHETIC-SAMPLE BIAS, not an inference bug (class direction / embedding extraction / phead checkpoint all cleared). Prevention: planner SKILL.md now requires a discriminating done-criterion for ML/classifier outputs grounded in a labeled ground-truth set with a known mixed distribution, located proactively at planning time (often the parent/source ticket's data).

### 2026-06-23 — 2026-06-23 — agentctl stage 6: 60 green tests masked an undefined helper on the unexercised failure path
- Where it arose: claude-agent-instructions: building the agentctl coordination engine (code-driven state machine replacing prose). A budget-truncated developer spawn left scripts/agentctl/cli.py cmd_dispatch CALLING _is_recursion_refusal(result) but never defining it — a latent NameError reachable only on a dispatch returncode!=0 branch.
- Working plan: The spawn's own report said 'done' and pytest reported 60 passed — the tempting close. The load-bearing axis was NOT 'tests pass' but 'is the helper actually defined / does the dispatch-failure path run'. No test exercised returncode!=0, so the NameError was invisible to the suite. Caught by independent on-disk verification (grep -n 'def _is_recursion_refusal' returned nothing while the call-site existed) instead of trusting the green run + self-report. Fix: a tightly-scoped cheap continuation spawn defined the helper (returncode==3 OR 'max-recursion-depth' in stderr, mirrored from spawn-specialist.py) AND added the missing failure-path test (60->72), then I re-ran pytest + verify-all + a real CLI dry-run + the hook deny/allow probe myself. Lesson: a passing suite proves only the paths it exercises; for code on an error/refusal branch with no covering test, grep that referenced symbols are defined and run the branch before concluding. And: after a spawn hits its --max-budget-usd ceiling, treat its work as INCOMPLETE+uncommitted and verify the tree on disk (git status + grep callees) rather than reading the self-summary.

### 2026-06-30 — arc-pr-gate fix: two developers' COMPLETED reports vs the actual trunk wiring; a green test that isn't load-bearing
- Where it arose: claude-agent-instructions / arcadia_claude_local: fixing the Layer-B bypass-gate classifier (arc-change-class.py _collect_paths parsed plain 'arc status' prose as changed paths -> clean-tree owner-junk PR misclassified non-junk)
- Working plan: Three surface-vs-load-bearing splits, each resolved by observing the real axis. (1) Developer COMPLETED report != on-disk wiring: dev #1 added a parse_status_short helper + pure-helper unit tests and landed them, but never rewired _collect_paths (still called plain 'arc status' with an inline parts[-1] parser); caught only by independent live reconstruction (temp branch off trunk + committed junk file -> classifier printed non-junk). Routed stage failed -> difficulty/replan with a mandatory END-TO-END test + live proof. (2) A stale working tree != trunk: my own 'PASS:37, no E1/E2' finding (grep of the local tree) wrongly concluded the e2e tests were missing -- they were already on trunk; an 'arc pull trunk' + rebase surfaced dev #2's E1/E2. The load-bearing axis for 'what is landed' is the synced trunk ref (arc show arcadia/trunk:<path>), not an un-pulled local checkout. (3) A green integration test != a load-bearing one: dev #2's e2e tests passed but their fake arc returned exit-1 for plain 'arc status', so they MISSED the exact original regression (plain-status prose). Proven by mutation: reverting _collect_paths to plain status + inline parts[-1] left dev #2's tests green but made my prose-emitting E1/E2 FAIL (41->38/3). Only mutation testing told which test actually guards the wiring. Delivered: consolidated single e2e section (fake arc on PATH, no ARC_PR_GATE_PATHS seam) driving the REAL _collect_paths -- E1 reconstructs the original bug, E2 reads --short porcelain, E3 discriminates a non-owner path; landed PR 14179130 merged as r20157883, suite 41/0, live reconstruction prints sandbox.
### 2026-06-30 — arc mount-layout restructure: green tests, but the agent's own permission globs were a missed path-representation
- Where it arose: arcadia_claude_local junk/the0/agents: collapsing the 3-mount arc layout to a single configurable anchor under `~/task-mounts/` + a `migrate-mount-layout.sh`
- Working plan: Stages 1–4 were green on every static axis (hermetic test 26✓, onboard-hook 16✓, deny-scan selftest 12✓, Core guard pytest 21✓, migration dry-run exit 0) — the tempting close. But changing the canonical mount **paths** is a canonical-identifier change, and a co-located representation static tests never load was missed: `projects/robot/deepagent/settings.project.json` (the file symlinked to `settings.local.json`) hard-codes the **absolute** old mount paths in its `Edit(...)`/`Write(...)` permission globs (`…/arcadia_claude_local/junk/…` and `…/arcadia_DEEPAGENT-*/robot/deepagent/**`). After the migration relocates those mounts under `~/task-mounts/`, the globs no longer match → the agent would hit permission prompts editing **its own storage and ticket trees** — a runtime-only regression invisible to the suite. The code-reviewer pass (not the tests) surfaced it; fix swept both the live per-project file and the `common/templates/` scaffold to `~/task-mounts/...`, verified by reading the merged file back **on trunk** (`arc show trunk:…/settings.project.json` → task-mounts, zero stale refs). Lesson: when a change moves canonical paths/names, the sweep must include the agent's **own config that encodes those paths** (permission globs, settings, symlink targets), not just product call-sites; and the load-bearing confirmation of "landed" is the file read on the synced trunk ref, not the spawn's self-report. Landed PR 14187754 → r20163718.


### 2026-07-01 — arc launcher false mount-failure: a green hermetic test whose stub emitted an idealized success token real arc never prints; plus a stray untracked Core builtin silently shadowing the documented machine-local plugin
- Where it arose: claude-personal/claude-task launcher — junk/the0/agents/common/project-entry arc workspace backend; DEEPAGENT-440 --new
- Working plan: User ran claude-personal --new; arc mount SUCCEEDED (printed '[mounted, pid: 3052100] mount: ...') but the backend judged success by grepping literal '[mounted]', which never matches the pid form -> 'mount did not report [mounted]', checkout -b skipped, mount leaked. Two load-bearing-axis misses. (1) The success-token grep asserted an IDEALIZED format, not the token the tool actually guarantees ('[mounted'). Fix: grep -qE '[mounted[],]' (accepts '[mounted]' and '[mounted, pid: N]'). (2) The hermetic test was GREEN yet not load-bearing: its arc stub printed the fake '[mounted]' string, so the suite exercised a format real arc never emits and could never have caught the bug. Fix: stub now emits realistic '[mounted, pid: 12345] mount: DIR' — same mutation-testing lesson as the 2026-06-30 arc-pr-gate context (a green test reproducing an idealized input is not a guard). (3) A deeper miss surfaced only because the fixed test then FAILED (26/27): an untracked Core builtin scripts/project_entry/backends/arc.sh (older worktree-style impl, never committed) was SHADOWING the intended machine-local storage plugin via the registry's builtin-first resolution — registry.sh's own doc names arc as the canonical plugin-only backend (only git.sh is a tracked builtin). The load-bearing axis for 'which backend runs' is registry_resolve_workspace arc in the real env, not 'my edited storage file exists'. Removed the stray (backup to /tmp), 27/27, resolution -> storage plugin. Cleaned the leaked DEEPAGENT-440 mount+store (arc unmount --forget). Landed to arc trunk: PR 14200982 published+self-merged (junk/the0 self-ship), commit f542e6c8 top of the path history on arcadia/trunk, verified by arc show trunk:PATH (grep fix at :143, realistic stub at :45) and arc mount --list = 0 DEEPAGENT-440 entries.
### 2026-07-16 — dead Write(...) permission rules: an invented provenance, an instance fixed while its generator lived, and a gate I authored but never ran
- Where it arose: junk/the0/agents — the harness warned at startup that four `Write(path)` allow-rules in `settings.project.json` are ignored by file permission checks (`Edit` rules cover every file-editing tool). Same file, same globs as the 2026-06-30 context above — which swept those globs for the mount-path rename but left the `Write` lines standing.
- Working plan: Three surface-vs-load-bearing misses, each surfaced by someone other than the check I was relying on.
  (1) **Invented provenance.** I reported the lines as "a leftover from the old syntax" — I had not read the history; it *sounded* like the kind of thing that would be true. The user asked how they got there. `arc log` shows only 4 commits and the initial one already carries `Edit`+`Write` pairs: the claim was fabricated, and the load-bearing axis (the history) was one command away. A plausible causal story is not evidence, and stating one in a user-facing report is worse than saying "I don't know" — it spends the user's trust on a guess.
  (2) **Instance fixed, generator alive.** I deleted the 4 rules from the deepagent instance, landed it (r20351842), closed the task. But `common/templates/settings.project.json` + `new-project.sh:48-50` sed-instantiate that template for EVERY new project, so each new project was still born with dead rules and the startup warning. The user's question — "не нужно ли было заменить их на адекватную реализацию замысла?" — was the trigger.
  <!-- Language exception: the user's trigger question is quoted verbatim in the original Russian; paraphrasing it into English would lose the exact wording the lesson turns on. -->
 Its literal answer was "the intent is already fully implemented by the `Edit` lines; the `Write` lines were redundant duplicates, not a half-done implementation" — but the instinct behind it found the real defect: I had fixed the морфология and never asked what functional place produced it ([[function-place-difficulty]]). **Closing a task is not proof it is closed; a difficulty with a live generator is not resolved, only postponed.**
  (3) **The gate on the gate.** The stage's `verify_command` used `grep -c 'FAIL '` — but `bad()` prints via `printf '  \033[31mFAIL\033[0m %s\n'`, so the literal text is `FAIL`+ESC and the pattern matches nothing on any tree. The clause was **constant-valued: it could not discriminate in either direction** (`-eq 1` fails on a healthy tree, `-eq 0` would pass on a broken one). I caught it only because I finally *ran* it — and I had inscribed the governing norm ("a guard is not trusted until shown to FAIL on a deliberate break") into that same plan, then applied it to the test assertion while exempting the instrument that judges the test. Re-selected to parse the suite's own uncoloured summary (`PASS: N   FAIL: M`) — the same `$FAIL` the script judges itself by — and mutation-proved the command itself. A reviewer then found the half I still hadn't guarded: the gate pinned the *absence* of `Write` but not the *survival* of the 3 `Edit` rules and placeholders, so a tree with an `Edit` line deleted would have passed a gate whose own criterion required them intact.
- Lesson: **an unexecuted verify_command is an untested assertion promoted to arbiter** — its most likely failure (a pattern that never matches: ANSI colour, changed output format, a renamed label) is silent and blind in both directions. Author it, then run it on both sides before binding it into a plan. And a gate must guard both halves of its criterion — what must disappear *and* what must survive; pinning only the negative half certifies trees that violate the positive one.
- Delivered: template cleaned to 0 `Write` / 3 `Edit` + a `no dead Write(...) permission rules` regression assertion in `test-composition.sh` (34 PASS / 1 FAIL vs the trunk baseline 33/1; the single FAIL is the pre-existing T5 `clean tree reports no issues`, measured on trunk with both files reverted — reported to the user, not absorbed).

## Common core & variations
**Common:** trust a *direct observation of the axis that carries the conclusion*, not a proxy that merely correlates with it. Mirrors [[2026-05-26-agent-system-plan-vs-reality-drift]]'s static-vs-runtime verification trap.

**Variations:**
- *External claim* → verify against the primary source (official docs), cheaply and decisively (one targeted `WebFetch`).
- *Health proxy* → probe the actual subsystem (background loops, disk), not the supervisor's liveness flag; retry transient blips before concluding outage.
- *Inherited assumption* → measure before optimizing; a small aggregator over real data can overturn a whole prior plan.

## Cost
- Both in-thread. 2026-06-04 used two background research sub-agents (Explore ~84k tok / general-purpose web ~94k tok) + 3 `WebFetch` doc-verification calls; the verification pass was the decisive cost driver and prevented committing two unverified env vars. ~25 min wall-clock each.
- 2026-06-05 ≈ 25 min; cost concentrated in the dateless-log time-windowing (no journal capture → `tail` + `awk` epoch conversion, `mawk` not `gawk`).
