Measured 2026-08-11 on scripts/agentctl/: 1375 hash-comment lines and 3231 docstring lines against 11139 code lines — 37% of non-blank lines are prose. Across scripts/ overall the hash-comment share is 8% (3394 / 40361), so the mass is concentrated in the engine rather than spread. Grounding incident: cmd_replan carried a comment that had been factually FALSE for two commits (it claimed _plan_content_digest excludes means/method/conditions/invariants/verify_command, while stage_question_key covers all five) — confidently-wrong documentation sitting beside correct code, which is the failure mode the density creates. Scope note for whoever plans this: docstrings are not interchangeable with comments here — many carry contracts that tests already bind (e.g. _plan_file_sha256's best-effort empty-string return), so a flat reduction would delete specifications along with restatement. Proposed shape: delete hash-comments aggressively; keep only docstrings that state a contract not derivable from the signature; move any checkable invariant into a test or verify-agentctl.py, where it fails on drift instead of rotting silently.
