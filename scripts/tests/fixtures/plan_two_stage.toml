[meta]
# Declared, and declared as what it is: a two-stage demo document with none of the
# substantive apparatus. The submission seam refuses SILENCE from a substantive session,
# not a plan that says what it is.
weight_class = "small_change"
task_id = "demo-two-stage"
goal = "Demonstrate the full two-stage coordination cycle"
done_criterion = "both stages PASSED and resolution confirmed"
criterion_type = "measurable"

[[stage]]
index = 1
title = "Scaffold module"
executor = "spawn:developer"
expected_result_image = "module file exists and imports cleanly"
criterion_type = "measurable"
done_criterion = "python -c 'import mod' exits 0"
depends_on = []
output_artifacts = ["mod.py"]

[[stage]]
index = 2
title = "Add tests"
executor = "spawn:developer"
expected_result_image = "pytest green for the new module"
criterion_type = "measurable"
done_criterion = "pytest tests/test_mod.py green"
depends_on = [1]
output_artifacts = ["tests/test_mod.py"]
