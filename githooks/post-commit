#!/usr/bin/env bash
# Push requires user confirmation — see skills/self-improvement/policy.md § Git sync
set -euo pipefail
echo "claude-agent-instructions: commit recorded. Push only after user confirms: scripts/sync-instructions-repo.sh push" >&2
