#!/usr/bin/env bash
# Auto-push after every commit in this repo (see skills/self-improvement/policy.md § Git sync)
set -euo pipefail
REPO="$(git rev-parse --show-toplevel)"
exec "$REPO/scripts/sync-instructions-repo.sh" push
